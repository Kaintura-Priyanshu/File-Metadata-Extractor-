# Core parsing libraries
Pillow==10.1.0
python-magic==0.4.27
mutagen==1.47.0
pypdf==3.15.4
python-docx==0.8.11
openpyxl==3.1.2
python-pptx==0.6.21

# Note: python-magic requires the system libmagic library.
# On Debian/Kali install it with:
#   sudo apt install libmagic1
#
# Video metadata (video_parser.py) shells out to the `ffprobe` binary
# rather than a Python package. Install it with:
#   sudo apt install ffmpeg
